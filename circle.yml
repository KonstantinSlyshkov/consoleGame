machine:
  java:
    version: oraclejdk8

test:
  override:
    # default CircleCI test action is 'mvn integration-test'
    - mvn test

deployment:
  master:
    branch: master
    commands:
      - curl GET https://circleci.com/api/v1.1/me
      - mvn install


