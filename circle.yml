machine:
  java:
    version: oraclejdk8

test:
  override:
    # default CircleCI test action is 'mvn integration-test'
    - mvn test

deployment:
  master:
    branch: master
    commands:
      - curl -v -X POST https://circleci.com/gh/KonstantinSlyshkov/getftpfeedswithnotifications/tree/master?circle-token=test
      - mvn clean install